type Player{
    id: ID!
    name : String
    team: Team
    city:String
}
enum Team{
    CSK
    MI
    RCB
    DC
    GT
    GST
}

type Query{
    findAll: [Player]
    findById(id: ID!): Player
}